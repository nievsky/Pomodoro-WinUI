00_Reference_CompletedApp

Place the full working Pomodoro project here for students to inspect when they are stuck. To prepare:

1. Copy the complete current project folder (the `Pomodoro` project) into this folder and rename the project to `00_Reference_CompletedApp` inside the `.csproj` and solution.
2. Ensure the package identity and manifest are adjusted if necessary.

This folder should contain a fully working app — do not break anything here.
